{
  "name": "hindsight",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered activity tracking for macOS using LM Studio",
  "workspaces": [
    "packages/api",
    "packages/image-summarizer",
    "packages/plugin",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": "^22.21.1",
    "npm": ">=8"
  },
  "os": [
    "darwin"
  ],
  "license": "MIT"
}
